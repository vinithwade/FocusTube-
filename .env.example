# FocusTube runs fully locally — no API keys required.
# The local embedding model (~80MB) downloads automatically on first search.

# Optional: API URL for extension (default: http://localhost:3000)
# FOCUSTUBE_API_URL=http://localhost:3000
